# uTools插件\-颜色值转换器产品需求文档（PRD V1\.0）

## 文档基础信息

|项目名称|uTools颜色值转换器插件|
|---|---|
|文档版本|V1\.0|
|撰写人|资深产品经理（20年大厂产品经验）|
|撰写日期|2026\.06\.17|
|适用开发工具|VibeCoding|
|开发依据|uTools官方开发者文档：https://www\.u\-tools\.cn/docs/developer/basic/getting\-started\.html|
|技术架构|Vue3 \+ JavaScript \+ SCSS \+ Node\.js（uTools原生能力）|
|适配平台|Windows、MacOS、Linux全平台uTools客户端|

## 一、项目概述

### 1\.1 应用基础信息

- **应用名称**：颜色值转换器

- **插件唯一标识**：color\_converter（遵循uTools小写蛇形命名规则）

- **应用简介**：面向设计师、前端开发、美工人群，一站式多格式颜色值互转、图片取色、标准色查阅、色彩科普轻量化uTools插件，依托uTools Node\.js本地原生能力\+前端网页架构开发，免打开第三方软件，全局快捷调用。

- **目标用户**：UI/视觉设计师、前端开发工程师、新媒体美工、平面设计从业者

### 1\.2 开发约束（强制遵循uTools开发规范）

1. 架构约束：严格遵循uTools插件底层架构，采用【Node\.js本地原生能力\+Vue3前端网页】架构开发，可调用Electron、Node\.js本地文件读取、图片解析原生API；

2. 环境约束：开发必须依赖正式版uTools客户端、uTools开发者工具，代码编辑器适配VSCode/WebStorm；

3. 配置约束：需配置标准plugin\.json文件，完善插件名称、描述、入口页面、preload预加载文件、适配平台、快捷唤醒指令；

4. 交互约束：适配uTools弹窗尺寸、全局唤起、收起交互逻辑，插件页面禁止脱离uTools独立弹窗运行。

### 1\.3 颜色适配类型（全覆盖）

支持双向互转：HEX、RGB/RGBA、HSL/HSLA、CMYK、LAB、HSV/HSB，包含透明度数值同步换算。

## 二、全局产品规则

### 2\.1 通用交互规则

1. 复制功能：所有输出颜色值、预设色值，支持一键复制，复制成功弹窗轻提示「复制成功」；

2. 输入容错：自动过滤颜色值空格、大小写、多余符号，自动识别缩写HEX（3位）并补齐6位HEX；

3. 预览规则：任意颜色值输入/选取后，页面实时展示纯色预览色块，直观查看色彩效果；

4. 页面布局：插件分为四大Tab页：预设色库、颜色互转、图片取色、色彩知识库，Tab切换无加载卡顿。

### 2\.2 唤醒规则

uTools全局唤起关键词：颜色转换、取色、色值换算，输入任意关键词可直接唤起插件主界面。

## 三、详细功能需求（四大核心模块）

### 模块一：英文预设颜色表功能

#### 3\.1\.1 功能描述

内置全网通用标准英文命名预设色库，支持浏览、搜索、一键复制全格式色值，满足设计常用基础色快速取用。

#### 3\.1\.2 功能细则

1. 色库体量：内置140\+W3C标准英文预设颜色，包含red、blue、snow、lightgray等官方命名色；

2. 展示内容：每一个预设色展示：英文色名、预览色块、默认HEX值；

3. 操作能力：
        

    - 关键词搜索：支持输入英文色名模糊检索对应颜色；

    - 点击展开：点击单个颜色卡片，展开该颜色全部格式色值（HEX/RGB/HSL等）；

    - 一键复制：任意格式色值独立复制按钮；

4. 排序规则：默认按英文首字母A\-Z排序，支持按色系分组筛选（红、橙、黄、绿、蓝、紫、黑白灰）。

#### 3\.1\.3 异常规则

无搜索结果时，页面展示空状态文案：暂无匹配预设颜色，请更换关键词。

### 模块二：自定义颜色值双向转换功能

#### 3\.2\.1 功能描述

用户手动录入任意合规颜色值，系统实时自动换算其余全部格式色值，支持全格式一键复制，支持透明度自定义修改。

#### 3\.2\.2 输入规则

1. 支持输入格式：HEX\(\#fff/\#ffffff\)、RGB\(255,255,255\)、RGBA、HSL、HSLA、CMYK、LAB、HSV/HSB；

2. 智能识别：系统自动识别用户输入格式，无需手动选择输入类型；

3. 透明度联动：修改RGBA/HSLA透明度参数，所有带透明格式色值同步联动变更。

#### 3\.2\.3 输出规则

1. 输入完成后毫秒级输出全部8类标准色值；

2. 每一类色值配套独立【复制】按钮；

3. 提供清空输入按钮，一键清空所有输入、换算结果、预览色块。

#### 3\.2\.4 容错校验

输入非法色值时，输入框标红提示「输入无效颜色值，请检查格式」，停止换算输出。

### 模块三：图片解析取色功能

#### 3\.3\.1 功能描述

依托uTools Node\.js本地文件读取原生能力，支持本地图片上传、粘贴图片，自动提取图片内主色、多点色值，支持色值复制。

#### 3\.3\.2 上传方式（双模式）

1. 点击上传：调用本地文件浏览器，选择本地图片文件；

2. 粘贴上传：支持剪贴板直接粘贴截图、图片文件，快速读取；

#### 3\.3\.3 支持图片格式

JPG、PNG、GIF、WebP、BMP，单张图片大小限制≤20MB（适配uTools内存管控规则）

#### 3\.3\.4 取色能力

1. 智能主色提取：自动识别图片占比TOP6主色调，展示预览\+全格式色值；

2. 定点吸色：上传图片后开启吸管工具，鼠标点击图片任意位置，精准拾取单点颜色；

3. 结果操作：所有提取色值，支持单点一键复制全部格式色值。

#### 3\.3\.5 本地权限规则

复用uTools客户端本地文件读取权限，无需额外申请系统权限，图片仅本地解析，不上传云端，保障用户隐私。

### 模块四：色彩设计基础知识科普功能

#### 3\.4\.1 功能描述

内置轻量化设计色彩知识库，面向入门设计师，可视化展示基础色彩理论，无需跳转外部网页，插件内直接查阅。

#### 3\.4\.2 科普内容明细

1. 色光三原色知识点：RGB三原色原理、混色规则、适用屏幕设计场景说明；

2. 标准色环可视化：12色相标准色环图示、互补色/邻近色搭配规则；

3. 各类色值科普：HEX/RGB/CMYK/LAB适用场景、换算逻辑、使用区别（屏幕/印刷通用区分）；

4. 常用设计规范：网页安全色范围、透明度使用标准、印刷色彩避坑知识点。

#### 3\.4\.3 交互规则

图文结合展示，支持上下滚动阅览，知识点折叠/展开，界面简洁无广告、无外链跳转。

## 四、非功能需求

### 4\.1 性能需求

1. 插件唤起速度：uTools关键词唤起≤300ms；

2. 色值换算响应：输入完成实时换算，响应≤100ms；

3. 图片解析速度：20MB以内图片解析取色≤1s；

4. 内存占用：插件后台常驻内存≤80MB，符合uTools插件内存管控标准。

### 4\.2 兼容性需求

1. 兼容uTools V4\.0及以上全版本客户端；

2. 适配Windows10\+/MacOS12\+/主流Linux发行版；

3. 界面自适应uTools默认弹窗尺寸，支持弹窗拖拽缩放。

### 4\.3 视觉体验需求

1. 采用SCSS编写样式，适配uTools深色/浅色系统主题自动切换；

2. 按钮、输入框、卡片交互hover、点击态齐全，符合uTools原生控件视觉风格；

3. 色彩预览色块无边距失真，色彩还原度100%。

### 4\.4 安全需求

1. 所有图片解析、色值计算全部本地Node\.js运算，零云端上传；

2. 不读取用户本地除主动上传图片外的文件数据；

3. 不植入后台联网、埋点、广告代码，合规上架uTools插件市场。

## 五、uTools专属开发配置要求（开发必填）

### 5\.1 目录结构规范

遵循uTools标准插件目录：

```javascript
color_converter/
├── plugin.json //插件核心配置文件
├── preload.js //预加载文件，挂载Node.js图片、文件原生API
├── index.html //Vue3打包入口页面
├── assets/ //图标、色环、知识库图片资源
├── css/ //SCSS编译样式文件
└── dist/ //Vue3编译打包资源
```

### 5\.2 plugin\.json核心配置字段

```json
{
  "name": "颜色值转换器",
  "code": "color_converter",
  "description": "支持HEX/RGB/CMYK等多格式色值互转、图片取色、预设色查阅、色彩科普uTools工具",
  "version": "1.0.0",
  "main": "index.html",
  "preload": "preload.js",
  "platform": ["win32", "darwin", "linux"],
  "features": [{
    "code": "color_convert_entry",
    "cmds": ["颜色转换", "取色", "色值换算"]
  }]
}
```

## 六、业务验收标准

### 6\.1 预设色库验收

1. 内置140\+W3C标准英文色，可字母、色系筛选，英文搜索精准匹配；

2. 所有色值可一键复制，复制提示正常生效。

### 6\.2 色值转换验收

1. 8类色值双向换算数值精准，透明度联动无误；

2. 非法输入自动校验提示，缩写HEX自动补齐；

3. 全格式结果均可独立一键复制。

### 6\.3 图片取色验收

1. 支持上传/粘贴图片，合规格式均可解析；

2. 主色提取、定点吸色数值精准，色值可复制；

3. 图片数据仅本地处理，无联网上传行为。

### 6\.4 色彩知识库验收

1. 色环、三原色图文展示完整，知识点可折叠阅览；

2. 无外链、无弹窗广告，页面加载正常。

### 6\.5 插件合规验收

可通过uTools开发者工具校验打包，可正常上架插件市场，全平台唤起、运行无报错。

## 七、版本迭代规划

- V1\.0：完成四大基础核心功能，完成uTools合规打包上架；

- V1\.1迭代规划：新增自定义收藏色值、历史转换记录、配色生成拓展功能。

> （注：部分内容可能由 AI 生成）
